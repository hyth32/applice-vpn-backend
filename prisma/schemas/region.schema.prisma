model Region {
  id        Int      @id @default(autoincrement())
  name      String
  host      String?
  port      Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  prices    Price[]
  keys      Key[]
  orders    Order[]
}
